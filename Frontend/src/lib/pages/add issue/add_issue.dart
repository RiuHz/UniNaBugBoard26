import 'package:flutter/material.dart';

class AddIssuePage extends StatelessWidget {
  const AddIssuePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Aggiungi un Issue')
      ],
    );
  }
}
